# Neon Postgres connection string. Used by the Drizzle client
# (src/db/index.ts) at runtime and by drizzle-kit for the db:* scripts.
DATABASE_URL =

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY =
CLERK_SECRET_KEY =

DISCORD_BOT_TOKEN =

# Required at BUILD time as well as at runtime. The Stripe SDK throws on an
# empty API key when the client is constructed at module scope, so a build
# without this set will fail while collecting page data.
STRIPE_SECRET_KEY =
STRIPE_WEBHOOK_SECRET =

NEXT_PUBLIC_APP_URL =
NODE_ENV =
NETLIFY_URL=
